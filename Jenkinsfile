pipeline {
    agent any  // This will run on any available agent

    stages {
        stage("New Stage") {
            steps {
                echo "Running on any available agent"
            }
        }
    }
}
